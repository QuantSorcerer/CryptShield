CryptShield is a text file encryption project.
It generates a unique key, encrypts text files using a secure algorithm, and allows decryption with the key. This ensures data confidentiality and security.
